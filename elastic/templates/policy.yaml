kind: policy
name: elasticsearch-policy
origin: default
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.gvc }}/identity/{{ .Values.elasticsearch.identity.name }}
targetKind: secret
targetLinks:
  - //secret/elasticsearch-config
  - //secret/elasticsearch-init
